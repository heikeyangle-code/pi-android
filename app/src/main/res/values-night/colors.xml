<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      `values/colors.xml` carries the day value; this is pi's `dark` export page
      (`PiPalette.Dark.pageBg`). See that file for why the window ground can only
      follow the system night mode and not pi's theme setting.
    -->
    <color name="pi_window_background">#18181E</color>
</resources>
