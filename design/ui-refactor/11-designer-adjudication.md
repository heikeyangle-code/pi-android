# 11 · 需要设计师裁定的冲突（v2 与 pi 上游 / v2 自身矛盾）

> 来源：终审甲、乙的偏差清单里"施工方不该自己拍"的条目。规则：**外观与版式以 v2 为准**（用户选定并冻结的设计稿），**颜色与内容来源以 pi 为准**（用户既有红线：pi 取色/变色不许动 + 「上色全都修到和 pi 1:1」）。凡是两者冲突、或 v2 自己前后不一致的，一律列在这里，不擅自决定。
> 用途：用户装完最终 APK 截图后，交给 v2 设计师代理做验收评审时**一并裁定**这些条目。

## A. v2 与 pi 上游冲突（我按"颜色/内容听 pi"暂定，请设计师确认）

| # | 冲突点 | v2 画的是 | pi / 09 的结论 | 现状 | 我的暂定 |
|---|---|---|---|---|---|
| A-1 | **码块底色** | 码块有 `--card` 底、行内码有 `--info` 底 | pi 在这两个分支**不给底色**（09 的 D6 明令不许改回去） | 透明（按 pi） | 保持 pi |
| A-2 | **行内码字号** | `t13` | 09 定 12.5，与 pi 的映射一致 | 12.5（按 09） | 保持 09 |
| A-3 | **对话内搜索命中高亮** | `selectedBg` / `surf-high` 底 | pi 用 `searchMatchBg` / `searchMatchText`（09 定案） | pi 的两个令牌 | 保持 pi |
| A-4 | **「还有 N 行未显示」位置** | 页脚 | pi 原生放在 body（`renderers/read.ts:133-135`） | body（按 pi） | 交裁定 |
| A-5 | **思考块收起态行高** | HTML 是 `3×18` 色条 + `t14` | 旧文档 `01 §7.4` 写「收起一行 32dp」 | 已按 HTML（18） | 交裁定 |

## B. v2 自身矛盾（同一份稿子里对不上）

| # | 项 | 三种说法 | 现状 | 我的暂定 |
|---|---|---|---|---|
| B-1 | 状态行分隔符 | 注释里有 `·`；实现是 `gap:5` 无 `·`；phone01 截图无 | 无 `·`（gap 5） | 按截图/实现 |
| B-2 | 状态行 gap | 5 与 4 两处 | 5 | 1dp 无视觉信号，不动 |
| B-3 | 耗时刻度对照台 | `phone15` 的 `right` 用 `toFixed(3)`（`0.040s`），真卡手写 `132ms` / `0.4s` | 按真卡（`piFormatDuration`） | 按真卡 |
| B-4 | markdown 表格尺寸 | `.b-tbl` 单元 `6px 8px`；库默认 `tableCellPadding=16` | 8（取了横向轴，KDoc 说明为何不能表达两轴） | 交裁定是否要拆成两轴（需要库支持） |
| B-5 | markdown 表格列宽 | v2 未给；库 `tableCellWidth=160` | 库默认 | 交裁定（库内部语义在本机无法核对） |

## C. 已由用户/父代理裁决、不再讨论（避免评审时重复提出）

- **一屏一个 accent**：撤销（用户：那个不好维护）。
- **终端**：只留设置里一行入口，零视觉投入（用户：不用管了）。
- **项目现场的 git 区块**：不做（pi 无对应 RPC、工作区不是 git 仓库）。
- **冷启动默认新会话**（用户明确）。
- **导航三项 + 会话列表覆盖层 + 工作区=项目现场**（用户已定）。
- **等宽字体随包**（用户已定）。
- **被拒态用 `bodyOnTool`**（D2）。
- **设置命中行用 `selectedBg`**（D9）。
- **当前生效值 2px 条按"被显式写过"推导、不限制每组一条**（D10）。
- **扩展对话框与设置对话框合并成同一构件**（乙在做）。
- **顶栏 48dp / 底栏 56dp / 全篇 1dp 线**（D5、06 §2）。
