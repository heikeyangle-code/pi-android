# 11 · 需要设计师裁定的冲突（v2 与 pi 上游 / v2 自身矛盾）

> 来源：终审甲、乙的偏差清单里"施工方不该自己拍"的条目。规则：**外观与版式以 v2 为准**（用户选定并冻结的设计稿），**颜色与内容来源以 pi 为准**（用户既有红线：pi 取色/变色不许动 + 「上色全都修到和 pi 1:1」）。凡是两者冲突、或 v2 自己前后不一致的，一律列在这里，不擅自决定。
> 用途：用户装完最终 APK 截图后，交给 v2 设计师代理做验收评审时**一并裁定**这些条目。

## A. v2 与 pi 上游冲突（我按"颜色/内容听 pi"暂定，请设计师确认）

| # | 冲突点 | v2 画的是 | pi / 09 的结论 | 现状 | 我的暂定 |
|---|---|---|---|---|---|
| A-1 | **码块底色** | 码块有 `--card` 底、行内码有 `--info` 底 | pi 在这两个分支**不给底色**（09 的 D6 明令不许改回去） | 透明（按 pi） | 保持 pi |
| A-2 | **行内码字号** | `t13` | 09 定 12.5，与 pi 的映射一致 | 12.5（按 09） | 保持 09 |
| A-3 | **对话内搜索命中高亮** | `selectedBg` / `surf-high` 底 | pi 用 `searchMatchBg` / `searchMatchText`（09 定案） | pi 的两个令牌 | 保持 pi |
| A-4 | **「还有 N 行未显示」位置** | 页脚 | pi 原生放在 body（`renderers/read.ts:133-135`） | body（按 pi） | 交裁定 |
| A-5 | **思考块收起态行高** | HTML 是 `3×18` 色条 + `t14` | 旧文档 `01 §7.4` 写「收起一行 32dp」 | 已按 HTML（18） | 交裁定 |

## B. v2 自身矛盾（同一份稿子里对不上）

| # | 项 | 三种说法 | 现状 | 我的暂定 |
|---|---|---|---|---|
| B-1 | 状态行分隔符 | 注释里有 `·`；实现是 `gap:5` 无 `·`；phone01 截图无 | 代码里曾**多插**了 ` · `（与暂定相反），本轮已删掉，现为纯间隙 | 按截图/实现 |
| B-2 | 状态行 gap | 5 与 4 两处 | 5 | 1dp 无视觉信号，不动 |
| B-3 | 耗时刻度对照台 | `phone15` 的 `right` 用 `toFixed(3)`（`0.040s`），真卡手写 `132ms` / `0.4s` | 按真卡（`piFormatDuration`） | 按真卡 |
| B-4 | markdown 表格尺寸 | `.b-tbl` 单元 `6px 8px`；库默认 `tableCellPadding=16` | 8（取了横向轴，KDoc 说明为何不能表达两轴） | 交裁定是否要拆成两轴（需要库支持） |
| B-5 | markdown 表格列宽 | v2 未给；库 `tableCellWidth=160` | 库默认 | 交裁定（库内部语义在本机无法核对） |

## A2. 乙报的、需要裁定的其余条目

| # | 项 | 现状 | v2 | 为什么留给你 |
|---|---|---|---|---|
| A-06 | **会话落点卡文案**（phone30/32：「工作目录由本应用固定 / 与 pi 共用 · 以后可以在这里选目录」） | **未加** | 稿子里有 | 这两句在 `02-real-content.md` 里**查不到**（而 06 §7 声明内容 100% 来自它），且「以后可以在这里选目录」是对未来的承诺。按"不许编文案"没加 |
| A-07 | **审批画两遍** | 实现的是**阻塞对话框**（phone57 形态） | phone13 还有一张**对话内卡片**（`! 待决策` + 标题`(5s)` + 10 段倒计时 + 允许实底/拒绝描边 + 排队行在倒计时上方 + 页脚） | pi 协议里只有一种阻塞式请求，「卡片回答」会引入第二种语义（回答后不阻塞？），属产品决策 |
| A-08 | **终端行副行文案** | 长句（唯一解释"只有原版 TUI 能干的事"的地方） | 半句「需要终端的扩展在那边」 | 删掉等于把这些能力从界面上抹掉；D6 + `docs/settings-review.md` §9 支持「只说一次」 |
| A-09 | **设置搜索的层级** | 栈里独立一层（标题「搜索设置」+ 返回） | 首页的一个**状态**（标题仍是「设置」、右上搜索开关、输入框 autoFocus） | 合并要动导航与 `settingsFocus`/`backReturnsToSearch`，是结构决策 |
| A-10 | **Boot 期底栏** | 收窄为「只有对话页被 BootScreen 接管时收底栏」 | 整屏接管，**没有**底栏 | 已由父代理裁决并采纳收窄版：否则启动那几分钟会把用户锁死在那一屏（乙-19） |
| A-11 | **编辑器字段框体** | M3 `OutlinedTextField`（56 高、浮动 label） | 40 高、圆角 9、`surf-low` + 1px `borderMuted` 自绘框 | **已按 v2 落地**（终审乙 U-07：单行 44 / 圆角 9 / `surf-low` / 焦点 `borderAccent`；文本编辑器多行 minHeight 80；数字框等宽 17） | 已收口，评审时只需看效果 |

## D. 本轮新增的 v2 与 pi 冲突（父代理已按「数值与上色照 pi」暂定，请设计师确认）
| # | 冲突点 | v2 画的是 | pi 是 | 现状 | 我的暂定 |
|---|---|---|---|---|---|
| D-1 | **状态行的数值格式与 `(auto)` 位置** | 样本值写 `上下文 52%` / `$0.42`（整数百分比、两位金额），`(auto)` 未出现在行内 | `toFixed(1)`（`52.0%`）、`toFixed(3)`（`$0.006`），`(auto)` 挂在**窗口**之后 | 已按 pi 落地（`%.1f` + `%` + `%.3f` + `窗口 (auto)`） | 保持 pi |
| D-2 | **diff 正文的上色范围与省略行** | 只给**符号列**上色，正文用 `--text`（上下文用 `--body-on-tool`）；折叠行写 `… N 行未变` | **整行**上色（符号+行号+正文同一个 `theme.fg`）；省略行是 ` <pad> ...`，**不带行数** | 已按 pi 落地（整行上色；省略行按 context 渲染、数字列留空） | 保持 pi |

## B2 · 已由父代理裁决的（记录，不再讨论）

- **键盘弹出时底栏不收起**（乙的结论，我采纳）：`imePadding()` 已把整个 scaffold 顶到键盘上方，底栏不遮挡不重叠；底栏是切到工作区/设置的唯一入口，收起等于把导航拿走；且 `WindowInsets.isImeVisible` 在 **API 26–29 上不可靠**（core 的 `Impl<30` 用"非负"判可见），要按 SDK 分支写才能避免"永远收起"。若将来要改，改法与三条注意点见乙的交付。
- **终端页顶栏仍是 64dp**：D6 说终端实现一律不动，且 v2 没画这一屏。
- **`ui/blocks/**` 与 `ChatScreen` 的顶栏/间距**：甲已按 v2 收口（见 `ce2d815`/`966349b`）。

## C. 已由用户/父代理裁决、不再讨论（避免评审时重复提出）

- **一屏一个 accent**：撤销（用户：那个不好维护）。
- **终端**：只留设置里一行入口，零视觉投入（用户：不用管了）。
- **项目现场的 git 区块**：不做（pi 无对应 RPC、工作区不是 git 仓库）。
- **冷启动默认新会话**（用户明确）。
- **导航三项 + 会话列表覆盖层 + 工作区=项目现场**（用户已定）。
- **等宽字体随包**（用户已定）。
- **被拒态用 `bodyOnTool`**（D2）。
- **设置命中行用 `selectedBg`**（D9）。
- **当前生效值 2px 条按"被显式写过"推导、不限制每组一条**（D10）。
- **扩展对话框与设置对话框合并成同一构件**（乙在做）。
- **顶栏 48dp / 底栏 56dp / 全篇 1dp 线**（D5、06 §2）。
- **数值单表化**：`PiV2Layout` 已删除、`PiSpacing.screen`(16) 已退役，页面尺度只有 `PiSpacing` 一张表（D16/D17）。
- **对话框只有一个构件**：`ui/components/PiDialog.kt`，设置侧与扩展侧同壳（乙-09/10）。
- **会话树保留「分支 / 条目」切换**（真功能），但换成 v2 的 `PiSeg` 组件语言。
